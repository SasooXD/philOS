CPU 8086
BITS 16

start:
	PUSH AX
	PUSH BX
	PUSH CX
	PUSH DX
	PUSH SI
	PUSH DI
	PUSH BP
	PUSH DS
	PUSH ES
	PUSH SS

	MOV AL, 'c'
	OUT 0x02, AL
	
	MOV AL, 'i'
	OUT 0x02, AL
	
	MOV AL, 'a'
	OUT 0x02, AL

	MOV AL, 'o'
	OUT 0x02, AL
	
	POP ES
	POP DS
	POP BP
	POP DI
	POP SI
	POP DX
	POP CX
	POP BX
	POP AX
	
	IRET
